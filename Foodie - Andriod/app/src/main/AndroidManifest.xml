<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.foodie.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Foodie">
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Foodie.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:theme="@style/Theme.Foodie.NoActionBar" />
        <activity android:name=".CartActivity" android:theme="@style/Theme.Foodie.NoActionBar" />
        <activity android:name=".OrdersActivity" android:theme="@style/Theme.Foodie.NoActionBar" />
        <activity android:name=".ProfileActivity" android:theme="@style/Theme.Foodie.NoActionBar" />
        <activity android:name=".LoginActivity" android:theme="@style/Theme.Foodie.NoActionBar" />
        <activity android:name=".SignupActivity" android:theme="@style/Theme.Foodie.NoActionBar" />
        <activity android:name=".ChangePasswordActivity" android:theme="@style/Theme.Foodie.NoActionBar" />
    </application>

</manifest>
